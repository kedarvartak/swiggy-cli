# Backend

Python backend scaffold for the shared Swiggy service layer.

Planned responsibility:

- expose APIs for the frontend
- host shared workflow execution logic
- integrate with Swiggy MCP and persistence
- keep CLI and UI behavior aligned through a common backend
